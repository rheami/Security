parsing avec l outil libnessus : http://libnessus.readthedocs.org/en/latest/

installation :

$ git clone https://github.com/bmx0r/python-libnessus.git
$ cd python-libnessus
$ python setup.py install

note : je print les objets python pour ce script 
       il me reste a essayer le diff de cette library
       pour l interface on va choisir ce que lon affiche
